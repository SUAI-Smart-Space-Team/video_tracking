pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                   make image push VERSION=show BRANCH=null FILE=./Modules/show/Dockerfile
                   make image pull VERSION=show
                   '''
                   
               }
             }
          }
        }
}
