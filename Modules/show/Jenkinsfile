pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                   make image push VERSION=show 
                   make image pull VERSION=show
                   '''
                   
               }
             }
          }
        }
}
