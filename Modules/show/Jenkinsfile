pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                   make image VERSION=show BRANCH=null FILE=./show/Dockerfile
                   make image pull VERSION=show
                   '''
                   
               }
             }
          }
        }
}
