pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {

                   sh '''
                   ./tracker/build_docker.sh
                   #make image push
                   #make image pull
                   '''
               }
             }
          }
        }
}