pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {

                   sh '''
                   make image push VERSION=tracker_0.0.1
                   make image pull VERSION=tracker_0.0.1
                   '''
               }
             }
          }
        }
}
